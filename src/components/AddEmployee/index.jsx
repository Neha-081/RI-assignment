import React from "react";
import Navbar from "../Navbar";

const AddEmployee = () => {

    return ( 
        <div>
         <header>
       <Navbar heading="Add Employee Details" />
    </header>
    <main>
       {/* <button className='plus-button' onClick={() => setDetailPage("addEmployee")}>+</button> */}
    </main>
        </div>
        
    )
}

export default AddEmployee;